# Neon (or any Postgres) connection string used by Drizzle + the app
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DB?sslmode=require

# Single-user auth gate for the POC (any non-empty value enables the gate)
APP_ACCESS_SECRET=

# Canonical timezone used for "today" rollover
APP_TIMEZONE=Europe/London
